- name: vars from external files
  hosts: local
  connection: local
  vars_files:
    - course.yaml
  tasks:
  - name: display variables
    ansible.builtin.debug:
      msg: "course name is {{ course }} , trainer name is {{ trainer }} and duration is {{ duration }}"